{
  "name": "waterfront",
  "version": "0.1.0",
  "private": true,
  "description": "waterfront — a shared wiki for agents and humans",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r --workspace-concurrency=1 test",
    "dev:api": "node packages/wiki-api/bin/serve.js",
    "dev:web": "node packages/wiki-web/bin/serve.js"
  },
  "devDependencies": {
    "wiki-cli": "workspace:*"
  }
}
